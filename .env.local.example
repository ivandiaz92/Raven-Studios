# Site URL (used for SEO: Open Graph, canonical, etc.)
# In production, set to your live domain e.g. https://ravenstudios.com
NEXT_PUBLIC_SITE_URL=https://your-app-url.com

# Strapi API URL
# For local development, use: http://localhost:1337/api
# For production, use your Strapi Cloud (or other) API URL
NEXT_PUBLIC_STRAPI_API_URL=http://localhost:1337/api

# Strapi API token (from Strapi admin: Settings → API Tokens)
# Required if Project (or other) API permissions are restricted to authenticated access
STRAPI_API_TOKEN=

# Contact form (Resend) — required for the contact form to work
# Get an API key at https://resend.com/api-keys
RESEND_API_KEY=
# Email address where contact and quote submissions are sent
CONTACT_EMAIL_TO=hello@yourdomain.com
# Optional: different address for quote notifications (defaults to CONTACT_EMAIL_TO)
# QUOTE_EMAIL_TO=quotes@yourdomain.com
# Optional: "Display Name <sender@yourdomain.com>" (must be a verified domain in Resend)
# If unset, uses Resend's onboarding address for testing
# CONTACT_FROM=Raven Studios <contact@yourdomain.com>

